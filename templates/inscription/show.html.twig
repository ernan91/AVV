{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inscription.id }}</td>
            </tr>
            <tr>
                <th>DateInscrip</th>
                <td>{{ inscription.dateInscrip ? inscription.dateInscrip|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateAnnule</th>
                <td>{{ inscription.dateAnnule ? inscription.dateAnnule|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('inscription_index') }}">back to list</a>

    <a href="{{ path('inscription_edit', {'id': inscription.id}) }}">edit</a>

    {{ include('inscription/_delete_form.html.twig') }}
{% endblock %}
